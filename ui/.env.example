# =======================================================
# Vercel Environment Configuration for Agentry Frontend
# =======================================================
# 
# Copy this file to .env.local in your ui/ directory for Vercel deployment.
# These variables are injected into the frontend at build time.
#
# For Vercel: Add these in your project's Environment Variables settings.

# Backend API URL (your Azure backend URL)
# Example: https://agentry-backend.azurewebsites.net
NEXT_PUBLIC_API_URL=

# Optional: Explicit WebSocket URL (derived from API_URL if not set)
# Example: wss://agentry-backend.azurewebsites.net
NEXT_PUBLIC_WS_URL=
