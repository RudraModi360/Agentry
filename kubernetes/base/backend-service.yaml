apiVersion: v1
kind: Service
metadata:
  name: agentry-backend
  labels:
    app: agentry-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: agentry-backend
